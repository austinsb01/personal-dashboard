# goals

Longer-term goals with progress: a title, optional description, a 0-100 percent
progress value, and an optional target date. One vertical slice (schema, repo,
actions, validation, UI) for the dashboard's goals area.

## Contents

- `schema.ts` - the Drizzle `goals` table. The single source of truth that the
  repository and derived types build on.
